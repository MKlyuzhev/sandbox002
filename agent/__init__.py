"""Headless Lien analysis orchestrator (CLI + stub paper executor + paper walk).

Research / paper-journal only. Does not place broker orders.
`python -m agent.mt4_clear` wipes sandbox chart objects (display only).
"""
